package se.metria.mapcms.repository;

import org.springframework.data.jpa.repository.JpaRepository;
import se.metria.mapcms.entity.SprakEntity;

public interface SprakRepository extends JpaRepository<SprakEntity, String> {
}
