import { AppConfig, UrlConfig } from "../../../lib/config/config.service";

export const languageFilePaths: string[] = [
  "/assets/locales/xp/",
  "/assets/locales/",
];

export const appConfig: AppConfig = {
  appName: "Samrad",
  configuration: "samrad",
};

export const urlConfig: UrlConfig = {
  configurationUrl: null,
};
